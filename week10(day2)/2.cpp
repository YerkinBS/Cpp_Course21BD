#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v;
    int n;

    while(cin >> n) v.push_back(n);

    for(int i = 0; i < v.size(); i++) cout << v[i] << ' ';

    // int n; cin >> n;
    // if(find(v.begin(), v.end(), n) != v.end()) cout << "found";
    // else cout << "NOT Found!!!!";

    // for(int i = 0; i < v.size(); i++){}


    // sort(v.begin(), v.end());
}